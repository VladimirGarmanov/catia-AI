# Project-scoped Codex team configuration. Codex loads this after the project is trusted.
# The coordinator sees only catia-v5-inspect; the executor agent explicitly enables
# the full catia-v5 server in its own configuration.
[agents]
enabled = true
max_concurrent_threads_per_session = 6


